"use client"
import React from 'react'

const ClientBtn = () => {
  return (
    <div>
        <button onClick={()=> console.log("button clicked")}>client button</button>
      
    </div>
  )
}

export default ClientBtn
